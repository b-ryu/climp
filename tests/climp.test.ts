import climp from '../src';

describe('climp', () => {
  it('works', () => {
    // const cli = climp({
    //   commands: {},
    //   global: {
    //     args: {
    //       hello: {
    //         type: 'boolean',
    //       },
    //     },
    //   },
    // });

    // expect(cli()).toEqual('');

    expect(1).toEqual(1);
  });
});
